/* exported getLastChar */

// Define a function named 'getLastChar' which has a single parameter: 'string'
// Locate the last character of a string, using the length property and subtracting 1.
// Return the last character of the string from the funciton

function getLastChar(string) {
  return string[string.length - 1];
}
