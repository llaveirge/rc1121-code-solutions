/* exported getFirstChar */

// Define a function named 'getFirstChar' which has a single parameter: 'string'
// Locate the first character of a string using it's index number, 0
// Return the first character of the string at index 0

function getFirstChar(string) {
  return (string[0]);
}
