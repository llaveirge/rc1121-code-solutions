/* exported take */

// Define a function named 'take' which has 2 parameters: 'array' and 'count'
// Make a copy of the array items up to the 'count' parameter
// Return the array copy

function take(array, count) {
  return array.slice(0, count);
}
