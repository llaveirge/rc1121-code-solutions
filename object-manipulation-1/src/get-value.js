/* exported getValue */

// Define a function called 'getValue' which has a single parameter: 'object'
// Locate the 'key' parameter property of the object
// Return the property value

function getValue(object, key) {
  return object[key];
}
