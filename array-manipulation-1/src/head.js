/* exported head */

// Define a function named 'head' which has a single parameter: 'array'
// Locate the first element of the array parameter by using its index number
// Return the value at the first index of the array

function head(array) {
  return array[0];
}
