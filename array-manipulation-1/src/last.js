/* exported last */

// Define a function named 'last' which has a single parameter: 'array'
// Locate the last element of the array parameter by subtracting 1 from its length
/* Return the value at the last index of the array by referencing its index via
 the expression length - 1 */

function last(array) {
  return array[array.length - 1];
}
